spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trirang
    username: postgres
    password: postgres
  data:
    redis:
      host: localhost
      port: 6379

storage:
  local:
    upload-dir: ./uploads

logging:
  level:
    root: INFO
    com.trirang: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
